function renderCart(){const el=document.getElementById('cart'),c=getCart();if(!c.length){el.innerHTML='<div class="profile-card"><h2>Keranjang masih kosong</h2><a class="primary" href="restoran.html">Cari Makanan</a></div>';return}let total=0;el.innerHTML=c.map(x=>{const f=makanan.find(m=>m.id===x.id),sub=f.harga*x.qty;total+=sub;return `<div class="cart-item"><div><b>${f.emoji} ${f.nama}</b><br><span class="muted">${formatRupiah(f.harga)} × ${x.qty}</span></div><div class="cart-actions"><button onclick="removeFromCart('${f.id}')">Hapus</button></div></div>`}).join('')+`<div class="total">Total: ${formatRupiah(total)}<br><br><a class="primary" href="checkout.html">Lanjut Checkout</a></div>`}renderCart();