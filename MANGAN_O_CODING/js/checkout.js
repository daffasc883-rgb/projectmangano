const c=getCart(),total=c.reduce((s,x)=>{const f=makanan.find(m=>m.id===x.id);return s+f.harga*x.qty},0);document.getElementById('checkoutTotal').innerHTML='<h2>Total '+formatRupiah(total)+'</h2>';
function placeOrder(){const address=document.getElementById('address').value.trim();if(!address)return alert('Masukkan alamat pengantaran');localStorage.setItem('mangan_order',JSON.stringify({total,address,payment:document.getElementById('payment').value,status:'Sedang diproses',created:new Date().toLocaleString('id-ID')}));localStorage.removeItem('mangan_cart');location.href='pesanan.html'}